proxy_ssl_verify on;  # Enables verification of the upstream server's SSL certificate. Ensures the proxy only connects to trusted servers.
proxy_ssl_verify_depth 2;  # Sets the maximum verification depth for the SSL certificate chain. A depth of 2 allows the root certificate and one intermediate CA.
